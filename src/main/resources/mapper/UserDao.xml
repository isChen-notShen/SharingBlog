<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="group.blog.dao.UserDAO">
<!--    开启二级缓存-->
<!--    <cache/>-->

    <select id="queryUserById" resultType="User" parameterType="int">
        select id, name, password, description, role, last_login_host from USER where id = #{id}
    </select>

    <select id="queryUserByName" resultType="User" parameterType="String">
        select id, name, password, description, role, last_login_host from USER where name = #{name}
    </select>

    <update id="updateLastLoginHostById">
        update user set last_login_host = #{host} where id = #{id}
    </update>

</mapper>